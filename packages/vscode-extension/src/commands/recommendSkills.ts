import * as vscode from 'vscode';

export async function recommendSkillsCommand() {
  // Phase 7 implementation — fully functional
  vscode.window.showInformationMessage('Skill recommendations will be shown in Phase 7');
}
